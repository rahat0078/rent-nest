model Category {
    id          String @id @default(uuid())
    name        String @unique @db.VarChar(255)
    description String? @db.VarChar(255)

    property Property[]
    @@map("categories")
}
